# 2026-08-15 example-topic
# One line per event: time, agent, what, where, why.
# New file per session/topic. Old files are never edited.
- 10:02 implementer: extracted TokenService from auth.ts -> src/services/token.ts; reason: spec S-14
- 10:19 verifier-spec: FAIL — criterion 3 (refresh test) red; raw output attached
- 10:24 fixer: race in refresh mutex; fixed with single-flight; -> LEARNINGS L-31
- 10:31 both verifiers: PASS -> STATE.md updated
